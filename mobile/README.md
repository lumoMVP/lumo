# Hyperlocal Starter (Expo)

## Quick start

```bash
cd /Users/humzam./Cursor/Start-up/mobile
npm install
npx expo install expo expo-status-bar react react-native
npm run start
```

Open on iOS Simulator (i) or Android Emulator (a), or scan the QR with Expo Go.
